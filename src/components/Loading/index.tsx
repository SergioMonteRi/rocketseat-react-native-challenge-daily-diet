import { Container, LoadingIndicator, Text } from './styles'

export const Loading = () => {
  return (
    <Container>
      <LoadingIndicator />
      <Text>Loading...</Text>
    </Container>
  )
}
